apiVersion: v1
kind: Service
metadata:
  name: {{ include "greetingApi.fullname" . }}
  labels:
    {{- include "greetingApi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "greetingApi.selectorLabels" . | nindent 4 }}
