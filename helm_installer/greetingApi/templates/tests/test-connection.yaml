apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "greetingApi.fullname" . }}-test-connection"
  labels:
    {{- include "greetingApi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "greetingApi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
